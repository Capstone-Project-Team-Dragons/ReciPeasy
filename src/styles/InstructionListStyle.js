import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        marginLeft: 15
    },
    text: {
        fontSize: 14,
        margin: 5
    }
});

export default styles;
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        marginLeft: 5
    },
    textTitle: {
        fontSize: 20,
        fontWeight: "bold",
        margin: 5
    },
    addtional: {
        marginLeft: 5,
        marginRight: 5,
        fontWeight: "bold",
        borderStyle: "solid",
        borderRadius: 5,
        borderWidth: 1,
        padding: 5
    },
    textAddtional: {
        fontSize: 12,
        fontWeight: "bold"
    }
});

export default styles;